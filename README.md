# twyn-ios 1.0.0 — artifacts

Build output, on an orphan commit reachable only through the tag `1.0.0`. **Nothing here is
edited by hand**; it is produced by `scripts/release.sh` in
[twyn-ios-sdk](https://github.com/Varmeta-App/twyn-ios-sdk)@`79dad03162b6d8d650bc94ee283dd10c3cbd8af2`.

The podspecs CocoaPods resolves against live on `main`, which is this repository's other job: it
is also the private spec repo. The two trees are deliberately never in the same commit.
